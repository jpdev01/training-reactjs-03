import React, { Component, useState } from 'react';
import ReactDOM from 'react-dom';

export default class InsertRecords extends Component {
    render() {
        return (
            <div>
                insert Record
            </div>
        );
    }
}